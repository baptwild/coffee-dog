<div class="m_Card">
	<div class="m_Card_image">
		<img src="{{ asset(image) }}" alt="{{ title }}" loading="lazy"/>
	</div>
	<div class="m_Card_content">
		<h4 class="m_Card_title">{{ title }}</h4>
		<p class="m_Card_description">{{ description }}</p>
	</div>
</div>
